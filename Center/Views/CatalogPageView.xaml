<UserControl x:Class="Center.Views.CatalogPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <ResourceDictionary>
            <!-- Стиль для карточек услуг -->
        <Style x:Key="ServiceCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <!-- Стиль для кнопки бронирования -->
        <Style x:Key="BookButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2C4A6B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3D5A7A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для тэгов категорий -->
        <Style x:Key="CategoryTagStyle" TargetType="Border">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="0,0,5,5"/>
        </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="15" Background="#F8F8F8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Панель фильтров -->
            <RowDefinition Height="*"/>
            <!-- Список услуг -->
        </Grid.RowDefinitions>

        <!-- Панель поиска и фильтрации -->
        <Border Grid.Row="0" 
                Background="White" 
                CornerRadius="8" 
                Padding="15"
                BorderBrush="#E5E7EB"
                BorderThickness="1"
                Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Строка поиска -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Width="300" 
                             Height="40"
                             Padding="12"
                             FontSize="14"
                             BorderBrush="#D1D5DB"
                             BorderThickness="1"
                             VerticalContentAlignment="Center"
                             Background="White"/>
                    <Button Content="Поиск" 
                            Width="100" 
                            Height="40"
                            Margin="10,0,0,0"
                            Style="{StaticResource BookButtonStyle}"
                            Command="{Binding SearchCommand}"/>
                </StackPanel>

                <!-- Выпадающий список категорий -->
                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding AllCategories}"
                          SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                          Width="200"
                          Height="40"
                          Padding="10"
                          FontSize="14"
                          BorderBrush="#D1D5DB"
                          BorderThickness="1"
                          Background="White"
                          Foreground="#374151"
                          HorizontalAlignment="Right">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding Name}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Id}" Value="-1">
                                                <Setter Property="FontStyle" Value="Italic"/>
                                                <Setter Property="Foreground" Value="#718096"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>
        </Border>

        <!-- Основной контент -->
        <Grid Grid.Row="1">
            <!-- Список услуг -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding DisplayedServices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Карточка услуги -->
                            <Border Style="{StaticResource ServiceCardStyle}">
                                <Grid Margin="20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Иконка -->
                                    <Border Grid.Column="0" 
                                            Width="80" 
                                            Height="80" 
                                            Background="#EFF6FF" 
                                            CornerRadius="8"
                                            Margin="0,0,20,0">
                                        <TextBlock Text="🎯" 
                                                   FontSize="30"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Foreground="#2C4A6B"/>
                                    </Border>

                                    <!-- Информация -->
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Name}" 
                                                   FontSize="18" 
                                                   FontWeight="SemiBold"
                                                   Foreground="#2D3748"
                                                   Margin="0,0,0,10"/>

                                        <!-- Категории -->
                                        <WrapPanel Margin="0,0,0,15">
                                            <ItemsControl ItemsSource="{Binding Categories}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel Orientation="Horizontal"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <!-- Закрывающий тег был пропущен -->

                                                <ItemsControl.ItemTemplate>
                                                    <!-- Убрал лишний закрывающий тег перед этим -->
                                                    <DataTemplate>
                                                        <Border>
                                                            <Border.Style>
                                                                <Style TargetType="Border" BasedOn="{StaticResource CategoryTagStyle}">
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Name}" Value="Учеба">
                                                                            <Setter Property="Background" Value="#E6F5ED"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Name}" Value="Деньги">
                                                                            <Setter Property="Background" Value="#FFF0E6"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Name}" Value="Работа">
                                                                            <Setter Property="Background" Value="#F0E6FF"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Name}" Value="Спорт">
                                                                            <Setter Property="Background" Value="#E6F3FF"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Name}" Value="Семья">
                                                                            <Setter Property="Background" Value="#FFE6F0"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Name}" Value="Хобби">
                                                                            <Setter Property="Background" Value="#FFF6E6"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Name}" Value="Финансы">
                                                                            <Setter Property="Background" Value="#FFF0E6"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Name}" Value="Путешествия">
                                                                            <Setter Property="Background" Value="#E6F3FF"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                    <Setter Property="Background" Value="#F0F9FF"/>
                                                                </Style>
                                                            </Border.Style>
                                                            <TextBlock>
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding Name}" Value="Учеба">
                                                                                <Setter Property="Foreground" Value="#2F855A"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Name}" Value="Деньги">
                                                                                <Setter Property="Foreground" Value="#E07A5F"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Name}" Value="Работа">
                                                                                <Setter Property="Foreground" Value="#8B5CF6"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Name}" Value="Спорт">
                                                                                <Setter Property="Foreground" Value="#2563EB"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Name}" Value="Семья">
                                                                                <Setter Property="Foreground" Value="#DB2777"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Name}" Value="Хобби">
                                                                                <Setter Property="Foreground" Value="#D97706"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Name}" Value="Финансы">
                                                                                <Setter Property="Foreground" Value="#E07A5F"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Name}" Value="Путешествия">
                                                                                <Setter Property="Foreground" Value="#2563EB"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                        <Setter Property="Foreground" Value="#2C4A6B"/>
                                                                        <Setter Property="Text" Value="{Binding Name}"/>
                                                                        <Setter Property="FontSize" Value="11"/>
                                                                        <Setter Property="FontWeight" Value="Medium"/>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </WrapPanel>

                                        <!-- Цены и длительность -->
                                        <StackPanel Orientation="Horizontal">
                                            <StackPanel Margin="0,0,30,0">
                                                <TextBlock Text="Будни" 
                                                           FontSize="12"
                                                           Foreground="#718096"/>
                                                <TextBlock Text="{Binding WeekdayPrice, StringFormat={}{0:N0} ₽}"
                                                           FontSize="18"
                                                           FontWeight="SemiBold"
                                                           Foreground="#2D3748"/>
                                            </StackPanel>

                                            <StackPanel Margin="0,0,30,0">
                                                <TextBlock Text="Выходные" 
                                                           FontSize="12"
                                                           Foreground="#718096"/>
                                                <TextBlock Text="{Binding WeekendPrice, StringFormat={}{0:N0} ₽}"
                                                           FontSize="18"
                                                           FontWeight="SemiBold"
                                                           Foreground="#2D3748"/>
                                            </StackPanel>

                                            <StackPanel>
                                                <TextBlock Text="Длительность" 
                                                           FontSize="12"
                                                           Foreground="#718096"/>
                                                <TextBlock Text="{Binding Duration, StringFormat={}{0} мин.}"
                                                           FontSize="18"
                                                           FontWeight="SemiBold"
                                                           Foreground="#2D3748"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Кнопка бронирования -->
                                    <Button Grid.Column="2" 
                                            Content="Забронировать"
                                            Style="{StaticResource BookButtonStyle}"
                                            Width="150"
                                            Height="40"
                                            FontSize="14"
                                            VerticalAlignment="Center"
                                            Command="{Binding DataContext.BookServiceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Индикатор загрузки (триггер на IsLoading) -->
            <Border Background="#CCFFFFFF">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="0,0,0,10"/>
                    <TextBlock Text="Загрузка услуг..." 
                               HorizontalAlignment="Center"
                               FontSize="14"
                               Foreground="#4A5568"/>
                </StackPanel>
            </Border>

            <!-- Сообщение "Нет услуг" (триггер на количество) -->
            <Border Background="White" 
                    CornerRadius="8"
                    BorderBrush="#E2E8F0"
                    BorderThickness="1"
                    Padding="40">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding DisplayedServices.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="🎯" 
                               FontSize="48"
                               Foreground="#CBD5E0"
                               Margin="0,0,0,20"/>
                    <TextBlock Text="Услуги не найдены" 
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="#4A5568"
                               Margin="0,0,0,10"/>
                    <TextBlock Text="Попробуйте изменить параметры поиска"
                               FontSize="14"
                               Foreground="#718096"
                               TextAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
