<Window x:Class="Center.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">

    <Window.Resources>
        <!-- Конвертер для видимости -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Стиль для кнопок меню -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Боковое меню -->
            <ColumnDefinition Width="220"/>
            <!-- Основная область -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Боковое меню -->
        <Border Grid.Column="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Заголовок -->
                <Border Grid.Row="0" Padding="20,15" Background="#F8F8F8">
                    <TextBlock Text="Развлекательный центр" 
                               Foreground="#111827" 
                               FontSize="14" 
                               FontWeight="Bold"/>
                </Border>

                <!-- Пункты меню -->
                <StackPanel Grid.Row="1" Margin="10,20,10,0">
                    <Button Content="📋 Каталог услуг" 
                            Command="{Binding NavigateToCatalogCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCatalogSelected}" Value="True">
                                        <Setter Property="Background" Value="#EFF6FF"/>
                                        <Setter Property="Foreground" Value="#2C4A6B"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="📊 Мои заказы" 
                            Command="{Binding NavigateToOrdersCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsOrdersSelected}" Value="True">
                                        <Setter Property="Background" Value="#EFF6FF"/>
                                        <Setter Property="Foreground" Value="#2C4A6B"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="📈 Отчеты" 
                            Command="{Binding NavigateToReportsCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsReportsSelected}" Value="True">
                                        <Setter Property="Background" Value="#EFF6FF"/>
                                        <Setter Property="Foreground" Value="#2C4A6B"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <!-- Информация о пользователе -->
                <Border Grid.Row="2" Margin="10" Padding="15" Background="#F8F8F8" 
                        CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="{Binding CurrentUser.Name}" 
                                   Foreground="#111827" 
                                   FontWeight="Bold"/>
                        <TextBlock Text="{Binding CurrentUser.Role}" 
                                   Foreground="#6B7280" 
                                   FontSize="12"/>
                        <Button Content="Выйти" 
                                Margin="0,10,0,0" 
                                Padding="8,5"
                                Background="Transparent"
                                Foreground="#FFB23939"
                                BorderBrush="#FFCABABA"
                                BorderThickness="1"
                                Command="{Binding LogoutCommand}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="#DC2626"/>
                                    <Setter Property="BorderBrush" Value="#DC2626"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#FEE2E2"/>
                                            <Setter Property="Foreground" Value="#991B1B"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Основная область -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Шапка с заголовком страницы -->
            <Border Grid.Row="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="15,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Заголовок текущей страницы -->
                    <TextBlock Grid.Column="0" 
                               Text="{Binding CurrentPageTitle}"
                               FontSize="24" 
                               FontWeight="Bold"
                               Foreground="#2C4A6B"
                               VerticalAlignment="Center"/>

                    <!-- Кнопка профиля -->
                    <Button Grid.Column="1"
                            Content="👤 Профиль" 
                            Padding="10,5"
                            BorderBrush="#FF2D3748"
                            Foreground="#FF2D3748"
                            Background="Transparent"
                            BorderThickness="1"
                            Command="{Binding ShowProfileCommand}"/>
                </Grid>
            </Border>

            <!-- Область для контента -->
            <Border Grid.Row="1" Background="#F8F8F8">
                <ContentControl Content="{Binding CurrentPage}" />
            </Border>
        </Grid>
    </Grid>
</Window>
