<UserControl x:Class="Center.Views.ProfilePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Center.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            
            <Style x:Key="InfoCardStyle" TargetType="Border">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="#E5E7EB"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Padding" Value="20"/>
                <Setter Property="Margin" Value="0,0,0,15"/>
            </Style>

            <Style x:Key="LabelStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#6B7280"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Margin" Value="0,0,0,5"/>
            </Style>

            <Style x:Key="ValueStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#111827"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="Medium"/>
            </Style>

        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="#F8F8F8" Margin="15">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- Основная информация -->
                <Border Style="{StaticResource InfoCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Аватар -->
                        <Border Grid.Column="0"
                                Width="120"
                                Height="120"
                                Background="#2C4A6B"
                                CornerRadius="60"
                                Margin="0,0,30,0">
                            <TextBlock Text="👤"
                                       FontSize="60"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>

                        <!-- Имя и Email -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding UserName}" 
                                       FontSize="28" 
                                       FontWeight="Bold"
                                       Foreground="#111827"
                                       Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding UserEmail}" 
                                       FontSize="16" 
                                       Foreground="#6B7280"
                                       Margin="0,0,0,15"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Категория гражданина и скидка -->
                <Border Style="{StaticResource InfoCardStyle}">
                    <StackPanel>
                        <TextBlock Text="📋 Информация о категории" 
                                   FontSize="18" 
                                   FontWeight="SemiBold"
                                   Foreground="#111827"
                                   Margin="0,0,0,20"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Категория гражданина -->
                            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Категория гражданина" 
                                               Style="{StaticResource LabelStyle}"
                                               Grid.Column="0"/>
                                    <Button Grid.Column="1"
                                            Content="Изменить"
                                            Padding="10,5"
                                            Margin="10,0,0,0"
                                            Command="{Binding EditCategoryCommand}"
                                            Background="#2C4A6B"
                                            Foreground="White"
                                            BorderThickness="0"
                                            FontSize="12"
                                            FontWeight="SemiBold"
                                            Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="#2C4A6B"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCategoryEditing}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#3D5A7A"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                                <!-- Режим просмотра -->
                                <Border Background="#F0F9FF"
                                        BorderBrush="#2C4A6B"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="15">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCategoryEditing}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding CitizenCategory}" 
                                               Style="{StaticResource ValueStyle}"
                                               Foreground="#2C4A6B"/>
                                </Border>
                                <!-- Режим редактирования -->
                                <StackPanel Visibility="{Binding IsCategoryEditing, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <ComboBox ItemsSource="{Binding AllCitizenCategories}"
                                              SelectedItem="{Binding SelectedCitizenCategory, Mode=TwoWay}"
                                              DisplayMemberPath="Name"
                                              Background="White"
                                              BorderBrush="#2C4A6B"
                                              BorderThickness="1"
                                              Padding="15"
                                              FontSize="14"
                                              Foreground="#2C4A6B"
                                              Margin="0,0,0,10"/>
                                    <Button Content="Сохранить"
                                            Padding="12,8"
                                            Command="{Binding SaveCategoryCommand}"
                                            Background="#10B981"
                                            Foreground="White"
                                            BorderThickness="0"
                                            FontSize="14"
                                            FontWeight="SemiBold"
                                            Cursor="Hand"
                                            HorizontalAlignment="Left">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="#10B981"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#059669"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </StackPanel>

                            <!-- Скидка -->
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Ваша скидка" 
                                           Style="{StaticResource LabelStyle}"/>
                                <Border Background="#ECFDF5"
                                        BorderBrush="#10B981"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="15">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding DiscountPercent}" 
                                                   Style="{StaticResource ValueStyle}"
                                                   Foreground="#10B981"
                                                   FontSize="20"
                                                   FontWeight="Bold"
                                                   Margin="0,0,8,0"/>
                                        <TextBlock Text="%" 
                                                   FontSize="14"
                                                   Foreground="#059669"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Дополнительная информация -->
                <Border Style="{StaticResource InfoCardStyle}">
                    <StackPanel>
                        <TextBlock Text="ℹ️ Дополнительная информация" 
                                   FontSize="18" 
                                   FontWeight="SemiBold"
                                   Foreground="#111827"
                                   Margin="0,0,0,20"/>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Роль -->
                            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                                <TextBlock Text="Роль в системе" 
                                           Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text="{Binding UserRole}" 
                                           Style="{StaticResource ValueStyle}"/>
                            </StackPanel>

                            <!-- Пароль -->
                            <StackPanel Grid.Row="1">
                                <TextBlock Text="Пароль" 
                                           Style="{StaticResource LabelStyle}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border Grid.Column="0"
                                            Background="#F8F8F8"
                                            BorderBrush="#D1D5DB"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Padding="15"
                                            Margin="0,0,10,0">
                                        <TextBlock Text="{Binding PasswordDisplay}" 
                                                   Style="{StaticResource ValueStyle}"
                                                   Foreground="#374151"
                                                   FontFamily="Consolas"/>
                                    </Border>
                                    
                                    <Button Grid.Column="1"
                                            Width="40"
                                            Height="40"
                                            Margin="0,0,10,0"
                                            Command="{Binding TogglePasswordVisibilityCommand}"
                                            Background="#4A6FA5"
                                            BorderThickness="0"
                                            Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="#4A6FA5"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    CornerRadius="8"
                                                                    Padding="8">
                                                                <ContentPresenter HorizontalAlignment="Center"
                                                                                  VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#3D5A7A"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <TextBlock Text="👁" 
                                                   FontSize="18"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Foreground="White"/>
                                    </Button>
                                    
                                    <Button Grid.Column="2"
                                            Content="Изменить"
                                            Padding="15,10"
                                            Command="{Binding ChangePasswordCommand}"
                                            Background="#2C4A6B"
                                            Foreground="White"
                                            BorderThickness="0"
                                            FontSize="14"
                                            FontWeight="SemiBold"
                                            Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="#2C4A6B"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#3D5A7A"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
